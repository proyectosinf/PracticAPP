#include "App.xcconfig"

ASSETCATALOG_COMPILER_APPICON_NAME = AppIconPre

// Packaging
PRODUCT_BUNDLE_IDENTIFIER = com.mobivery.fct25.pre

// Signing
PROVISIONING_PROFILE_SPECIFIER = match AdHoc ${PRODUCT_BUNDLE_IDENTIFIER}

// Swift Compiler - Custom Flags
SWIFT_ACTIVE_COMPILATION_CONDITIONS = ADHOC
